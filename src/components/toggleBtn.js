import { acxLightLogo } from './../assets/icons';

/* 

.css-14old29 {
    position: fixed;
    z-index: 2147482999;
    transition: opacity 0.4s ease 0s, transform 0.3s cubic-bezier(0.24, 0.6, 0.35, 0.96) 0s;
    height: calc(100% - 40px);
    right: 20px;
    bottom: 20px;
    transform-origin: 100% 100%;
    opacity: 0;
    pointer-events: none;
    transform: scale(0.1);
}
*/

export default `
<button id="toggle-btn" class="toggle-btn">${acxLightLogo}</button>
`;